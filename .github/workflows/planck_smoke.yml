name: CLASS alive check

on:
  push:
  pull_request:

jobs:
  alive:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Build Docker image
        run: docker build -t class-alive-check .

      - name: Run alive check
        run: docker run --rm class-alive-check